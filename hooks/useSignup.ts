"use client";
import{useCallback,useState}from"react";import{useRouter}from"next/navigation";import*as authService from"@/services/auth.service";import{SignupPayload}from"@/types/auth";
export function useSignup(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);const signup=useCallback(async(payload:SignupPayload,options?:{redirect?:boolean})=>{setLoading(true);setError(null);try{await authService.signup(payload);if(options?.redirect!==false)router.push("/login");return true}catch(err){setError(err instanceof Error?err.message:"Une erreur inconnue est survenue.");return false}finally{setLoading(false)}},[router]);return{signup,loading,error};}