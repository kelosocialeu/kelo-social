"use client";
import{useState}from"react";import Link from"next/link";import AuthLayout from"@/components/layout/AuthLayout";import AuthSuccessAnimation from"@/components/auth/AuthSuccessAnimation";import Input from"@/components/ui/Input";import Select from"@/components/ui/Select";import Button from"@/components/ui/Button";import{useSignup}from"@/hooks/useSignup";import{useTranslation}from"@/components/providers/TranslationProvider";
const PROVIDERS=[{id:"kelo",label:"Kelo Social",url:"https://pds.kelosocial.eu"}];const GATE=(process.env.NEXT_PUBLIC_KELO_GATEKEEPER_URL||"https://pds.kelosocial.eu").replace(/\/$/,"");
export default function SignupPage(){const{t}=useTranslation();const[handle,setHandle]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[birthDate,setBirthDate]=useState("");const[pdsUrl,setPdsUrl]=useState(PROVIDERS[0].url);const[verificationError,setVerificationError]=useState("");const[verifying,setVerifying]=useState(false);const[success,setSuccess]=useState(false);const{signup,loading,error}=useSignup();
const complete=async(code:string)=>{const ok=await signup({handle,email,password,birthDate,pdsUrl,verificationCode:code},{redirect:false});if(ok)setSuccess(true)};const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();setVerificationError("");if(!handle||!email||!password||!birthDate)return;const full=handle.includes(".")?handle:`${handle}.kelosocial.eu`;const state=crypto.randomUUID();const callback=`${window.location.origin}/signup/gate-callback`;const gate=new URL(`${GATE}/gate/signup`);gate.searchParams.set("handle",full);gate.searchParams.set("state",state);gate.searchParams.set("redirect_url",callback);const popup=window.open(gate.toString(),"kelo-pds-verification","popup=yes,width=520,height=720");if(!popup){setVerificationError("Votre navigateur a bloqué la fenêtre de vérification.");return}setVerifying(true);const timeout=window.setTimeout(()=>{window.removeEventListener("message",onMessage);setVerifying(false);setVerificationError("La vérification a expiré.");try{popup.close()}catch{}},300000);async function onMessage(event:MessageEvent){if(event.origin!==window.location.origin)return;const data=event.data as{type?:string;code?:string;state?:string};if(data?.type!=="kelo-gatekeeper-verification"||data.state!==state||!data.code)return;clearTimeout(timeout);window.removeEventListener("message",onMessage);setVerifying(false);try{popup.close()}catch{}await complete(data.code)}window.addEventListener("message",onMessage)};
if(success)return <AuthSuccessAnimation title={t("auth.signup.successTitle","Compte créé !")} message={t("auth.signup.successMessage","Bienvenue sur Kelo Social. Votre compte est prêt, vous pouvez maintenant vous connecter.")} duration={2100} onDone={()=>window.location.replace("/login")}/>;
return <AuthLayout title={t("auth.signup.title","Inscription")} tagline={t("auth.signup.tagline","Rejoignez le réseau social souverain et décentralisé.")}><form onSubmit={handleSubmit} className="flex flex-col gap-4"><Select label="Hébergé sur le PDS" value={pdsUrl} onChange={e=>setPdsUrl(e.target.value)}>{PROVIDERS.map(p=><option key={p.id} value={p.url}>{p.label}</option>)}</Select><Input label="Date de naissance (18 ans minimum)" type="date" required value={birthDate} onChange={e=>setBirthDate(e.target.value)}/><div><Input label="Nom d'utilisateur (Handle)" type="text" required startAdornment="@" value={handle} onChange={e=>setHandle(e.target.value)}/><p className="mt-1 text-xs text-kelo-muted">Votre nom final sera @{handle||"votre-nom"}.kelosocial.eu</p></div><Input label="Email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/><Input label="Mot de passe" type={showPassword?"text":"password"} required value={password} onChange={e=>setPassword(e.target.value)} endAdornment={<button type="button" onClick={()=>setShowPassword(!showPassword)}>{showPassword?"Masquer":"Voir"}</button>}/><div className="rounded-2xl border border-kelo-border bg-kelo-background p-3 text-center text-xs text-kelo-muted">En cliquant sur S'inscrire, le captcha sécurisé du PDS Kelo Social s'ouvrira.</div>{(error||verificationError)&&<p className="text-center text-sm font-medium text-kelo-danger">{verificationError||error}</p>}<div className="mt-2 flex gap-4"><Link href="/login" className="flex w-1/2 items-center justify-center rounded-full bg-kelo-background py-3 font-bold">Déjà un compte ?</Link><Button type="submit" loading={loading||verifying} className="w-1/2">S'inscrire</Button></div></form></AuthLayout>}
